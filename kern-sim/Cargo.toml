[package]
name = "kern-sim"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "kern simulator: a deterministic pure-Rust SPI bus and display panel."

[dependencies]
