# Build artifacts: the compiled crate and the wasm-pack output. Both are
# regenerated by `npm run wasm` (prebuild) locally and in CI. Source + Cargo
# manifests + the lockfile are committed; nothing generated is.
/target
/pkg
